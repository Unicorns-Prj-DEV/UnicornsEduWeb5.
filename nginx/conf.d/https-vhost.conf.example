# Deprecated: production no longer terminates TLS in nginx.
#
# Cloudflare/cloudflared owns the public hostname and HTTPS certificate.
# Point the tunnel service to:
#
#   http://127.0.0.1:80
#
# The active nginx vhost is `app.conf`, bound by docker-compose to
# 127.0.0.1:80 on the VPS. Keep this file as a note only; do not add a
# domain-specific 443 server block unless the deployment leaves cloudflared.
