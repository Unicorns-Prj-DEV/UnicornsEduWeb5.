# TLS is terminated by Cloudflare/cloudflared.
# Keep this file loaded but empty so nginx no longer requires a domain-specific
# certificate on the VPS. The active local HTTP vhost lives in app.conf.
